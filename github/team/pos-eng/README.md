# POS engineering team

Creates the `pos-eng` GitHub team and assigns the first half of the users
managed by [`github/user`](../../user): users 01 through 10 in the example
configuration.

Replace the usernames in `terraform.tfvars` with the real accounts created
manually before applying.

```sh
terraform init
terraform plan
terraform apply
```
