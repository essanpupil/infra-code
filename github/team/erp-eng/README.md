# ERP engineering team

Creates the `erp-eng` GitHub team and assigns the second half of the users
managed by [`github/user`](../../user): users 11 through 20 in the example
configuration.

Replace the usernames in `terraform.tfvars` with the real accounts created
manually before applying.

```sh
terraform init
terraform plan
terraform apply
```
